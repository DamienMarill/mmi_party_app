import { AutoCropDirective } from './auto-crop.directive';

describe('AutoCropDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoCropDirective();
    expect(directive).toBeTruthy();
  });
});
